# 아직 안 고친 것

실사용에서 드러났지만 아직 손대지 않은 것들. **고칠 때마다 지운다.**
고친 것은 여기 남기지 않는다 — git 기록이 그 일을 한다.

각 항목은 **무엇이 문제인지**와 **어떻게 알았는지(실측)** 를 함께 적는다. 근거 없이
"이게 나을 것 같다"는 여기 적지 않는다.

---

## 1. 리드가 기다리는 중인데 "응답 없음"이라고 한다

**증상** — 팀원에게 일을 맡긴 동안 리드 캐릭터에 `⚠ 응답 없음`이 뜬다. 사람은
"뭔가 잘못됐다"로 읽는데 실제로는 **결과를 기다리는 중**이다.

**원인** — 5분 넘게 이벤트가 없으면 멈춘 것으로 본다
(`renderer/app.js`의 `STALL_MS = 300_000`). 그런데 리드는 **자기가 도구를 쓸 때만**
이벤트를 낸다. 팀원에게 맡긴 동안에는 기다리기만 하므로 아무것도 남지 않는다.

**실측** (daily 프로젝트, 지시 하나 109분):

| 리드가 조용한 시간 | 그동안 실제로 일한 사람 |
| ---: | --- |
| 9.0분 | 백엔드, 디자인 |
| 19.0분 | 프론트, 모바일 |
| 19.3분 | 리뷰, 모바일 |
| 35.2분 | 백엔드, 리뷰, QA |
| 19.4분 | 백엔드, 프론트 |

합계 **102분 — 전체의 93%**. 다섯 번 **모두** 팀원이 한창 일하는 중이었다.
한 번도 진짜로 멈춘 적이 없다. 팀원 하나가 30분씩 도는 이 앱에서 5분 기준은
**거의 항상** 걸린다.

**고칠 방향** — 팀원이 일하고 있으면 리드를 멈춘 것으로 보지 않는다. 대신 무엇을
기다리는지 보여 준다(`기다리는 중 · 백엔드·QA 결과 대기`). **정말 문제인 경우는
아무도 일하지 않는데 리드가 조용할 때뿐이다** — 그때만 경고한다.

---

## 2. 도구 결과가 길면 팀원이 오류를 받는다

**증상** — `Error: result (73,796 characters) exceeds maximum allowed` (frontend-dev).

**실측** — daily 프로젝트에서 1회. 팀원이 스스로 나눠 읽어 회복했고 작업은 이어졌다.

**모르는 것** — 무엇을 읽다 났는지 기록에 남지 않는다. 어느 도구·어느 파일이었는지
알 수 있으면 지침으로 막을 수 있다(예: "큰 파일은 범위를 잘라 읽어라"). 지금은
훅이 오류 본문만 남기고 **직전 도구 호출을 함께 남기지 않는다.**

---

## 3. 대화는 200줄까지만 보인다

저장은 그보다 많이 되는데 화면에는 마지막 200줄만 올라온다(`renderer/app.js`).
한 시간짜리 작업이면 앞부분을 볼 방법이 없다. 검색도 없다.

---

## 4. 스냅샷은 30개까지만 둔다

`SNAP_KEEP = 30`. 지시를 서른 번 보내면 그 앞으로는 되돌릴 수 없다. 실사용에서
아직 부딪힌 적은 없지만, 되돌리기가 유일한 안전장치라 언젠가 걸린다.
